export const BET_TYPE = {
  playerPair: 'playerPair',
  tie: 'tie',
  bankerWin: 'bankerWin',
  playerWin: 'playerWin',
  bankerPair: 'bankerPair',
};

export const MAX_CHIP_PER_COLUMN = 7;
export const CHIP_WIDTH = 26;
export const CHIP_HEIGHT = 18;
export const CHIP_WIDTH_MOBILE = 0.035 * Math.max(window.innerHeight, window.innerWidth);
export const CHIP_HEIGHT_MOBILE = 0.0207 * Math.max(window.innerHeight, window.innerWidth);
export const TRACKING_TABLE = 'TRACKING_TABLE';
export const CLEAR_ALL_CHIP = -1000;
